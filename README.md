# Personal Diary System

A Python project demonstrating file handling operations.

## Features
- Auto-detect and create diary file if not exists
- Write new entries with timestamp
- Read all past entries

## Concepts Used
- File Detection (`os.path.exists`)
- Write File (`open` with `"a"` mode)
- Read File (`open` with `"r"` mode)
